# Athena — Python dependencies
# Phase 1 (data & config foundation) only needs the data-generation stack.
# Pinned to the minimum the snapshot generator and config tooling require.
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
openpyxl>=3.1   # read/write .xlsx — snapshots can be emitted as CSV and/or XLSX

# Build Sequence 4 (narrative layer). Provider SDKs are imported lazily in app/llm/llm_client.py,
# so only the provider you actually use needs to be installed at runtime.
httpx>=0.27     # local Ollama adapter (native /api/chat)
anthropic>=0.40 # Anthropic (Claude) adapter — official SDK
openai>=1.40    # OpenAI (GPT) adapter — official SDK
